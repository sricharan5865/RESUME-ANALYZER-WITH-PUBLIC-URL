# BRIEFING — 2026-07-15T08:32:00Z

## Mission
Investigate the JD Match / RAG Search UI pages, settings, and general toggle configurations, and identify backend question generation routes.

## 🔒 My Identity
- Archetype: Codebase Explorer
- Roles: Read-only investigator, analyzer
- Working directory: c:\Users\sri charan\Documents\projects\hr recruter\.agents\teamwork_preview_explorer_ollama_opt_3_gen3
- Original parent: e3496418-aaab-4b82-8c2b-16e501309f85
- Milestone: Milestone 3 - JD Match and On-Demand Question Gen Exploration

## 🔒 Key Constraints
- Read-only investigation — do NOT implement
- Do not edit any code files
- Focus only on analyzing pages, toggles, backend schemas, API signature, and integration hooks.

## Current Parent
- Conversation ID: e3496418-aaab-4b82-8c2b-16e501309f85
- Updated: 2026-07-15T08:33:00Z

## Investigation State
- **Explored paths**:
  - `client/src/components/RAGSearch.jsx` (JD Match UI, on-demand question generation trigger `toggleQuestions`)
  - `client/src/components/PipelineBoard.jsx` (Candidate pipeline board, usage of `rankAccordingToJob` prop for candidate scoring and sorting)
  - `client/src/App.jsx` (Global toggle definition `rankAccordingToJob` and toggle state persistence to `/api/settings` backend endpoint)
  - `client/src/components/CandidateDetails.jsx` (Selection between JD match properties vs. profile category properties based on `rankAccordingToJob`)
  - `server/server.js` (Ingestion flow, `/api/candidates/:id/generate-jd-questions` and `/api/rag/jd-search` routes)
  - `server/models.js` (Mongoose schemas for `Candidate`, `Job`, and `Settings`)
  - `server/geminiParser.js` (`generateQuestionsForCandidate` core AI question generator logic)
- **Key findings**:
  - The global toggle `rankAccordingToJob` is a state managed in `App.jsx`, shared across `PipelineBoard.jsx`, `CandidateDetails.jsx`, `Dashboard.jsx`, and `TagSearch.jsx`. It changes sorting/display scores in the board and determines which evaluation details (JD match vs. profile category competency) are rendered in candidate details.
  - Candidate-job tailored questions are generated by the `/api/candidates/:id/generate-jd-questions` endpoint using `generateQuestionsForCandidate` in `geminiParser.js`.
  - Tailored questions are stored in the database in the `Candidate` schema under `jdQuestions` (including `hrQuestions`, `technicalQuestions`, and `jdTitle`).
  - There is no automatic tailored question generation on ingestion. Ingestion only parses general questions (not job-specific ones). Job-specific questions are generated on-demand (lazy-loaded when expanding candidate cards).
  - The API signature for on-demand JD match question generation has been verified.
- **Unexplored areas**: None. The investigation successfully answered all user tasks.

## Key Decisions Made
- Confirmed the exact endpoint signature and database mappings.
- Confirmed that the existing UI utilizes lazy loading for JD-specific questions to optimize performance and token usage.

## Artifact Index
- `c:\Users\sri charan\Documents\projects\hr recruter\.agents\teamwork_preview_explorer_ollama_opt_3_gen3\handoff.md` — Detailed investigation findings report.
