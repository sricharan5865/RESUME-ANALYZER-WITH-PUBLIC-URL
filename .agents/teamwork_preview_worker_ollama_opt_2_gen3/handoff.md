# Handoff Report

## 1. Observation
- Verified in `client/src/components/CandidateDetails.jsx` that `isGeneralRole` is defined as `!job` on line 446 and that the button text is exactly `"Construct questions according to JD Match"` on line 945.
- Checked `server/geminiParser.js` implementation:
  - `getRecruiterSystemInstruction(aiProvider)` returns `baseInstruction` for default providers and was updated to include:
    ```
    NEGATIVE CONSTRAINT: NEVER begin a question with introductory phrases like "Given your experience with...", "Since you worked at...", "According to your resume...", "In your role as...". Start the question directly.
    ```
  - For `aiProvider === 'ollama'`, a condensed prompt was implemented that defines the expected JSON sections (`career_gaps`, `technical_depth_audit`, `domain_question_bank`, `project_deep_dive`, `hr_questions`, `red_flags`, `must_prepare_topics`, `fit_summary`, and `projects`) and enforces:
    - `NEGATIVE CONSTRAINT: NEVER begin a question with introductory phrases like "Given your experience with...", "Since you worked at...", "According to your resume...", "In your role as...". Start the question directly.`
    - Question lengths must be strictly under 15-20 words.
  - Inside `generateQuestionsForCandidate(candidateProfile, jobDescription = null)`, both `if (aiProvider === 'ollama')` blocks (lines 1704-1723 and 1867-1877) were updated to enforce the negative constraint and the 15-20 words length constraint.
  - In `callAIProviderDirect` under `aiProvider === 'ollama'`, `dynamicNumPredict` is configured to `2048` (line 822) to match the parameter rules in `AGENTS.md`.
- Ran Vitest E2E tests:
  ```powershell
  npx start-server-and-test start:test http://localhost:5001/api/auth/status "npx vitest run ../tests/e2e/regenerateQuestions.test.js --config ../tests/e2e/vitest.config.js"
  ```
  Result:
  ```
  ✓ ../tests/e2e/regenerateQuestions.test.js  (10 tests) 729ms
  Test Files  1 passed (1)
       Tests  10 passed (10)
  ```

## 2. Logic Chain
1. Confirming the frontend is correct guarantees that the correct UI triggers for "Construct questions according to JD Match" are in place and that the check `!job` correctly sets `isGeneralRole`.
2. By modifying `getRecruiterSystemInstruction(aiProvider)` to return a condensed prompt when `aiProvider === 'ollama'`, we reduce prompt preprocessing latency on local Ollama setups, fulfilling the constraint of `<800` tokens and saving prompt overhead.
3. Adding the negative constraint and the strict 15-20 word count limits in both the system instructions and within the conditional provider blocks ensures that general candidate questions generated by both cloud providers and Ollama are short, direct, and free of introductory context-clutter.
4. Setting `dynamicNumPredict` to `2048` for complex schema parsing operations complies with the local model token optimization rules set out in `AGENTS.md`.
5. Running the E2E test file `regenerateQuestions.test.js` successfully validates that candidate questions are correctly generated, that the endpoints respond with 200, and that database updates are successfully persisted.

## 3. Caveats
- Tests were run with mock API handlers intercepting Gemini calls in the testing server environment (`testServerEntry.js`), so actual network requests were not sent to live Gemini endpoints.
- Real execution on local Ollama models requires a running Ollama service on `http://localhost:11434` with the specified model loaded.

## 4. Conclusion
The frontend is verified as correct, the backend prompt optimizations for Ollama and general questions have been successfully implemented, and E2E test verification passes cleanly (10/10 tests passed).

## 5. Verification Method
1. Inspect `server/geminiParser.js` to ensure system instructions include the negative constraint and condensed Ollama prompt template.
2. Run the target tests from the `server` directory:
   ```powershell
   npx start-server-and-test start:test http://localhost:5001/api/auth/status "npx vitest run ../tests/e2e/regenerateQuestions.test.js --config ../tests/e2e/vitest.config.js"
   ```
